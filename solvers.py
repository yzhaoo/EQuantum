import numpy as np

def local_solver(fsc,idx,Espacing=100):
    ildos=fsc.ildos.copy()
    ni=fsc.ni
    Ci=fsc.Ci
    sum(np.linspace(0,dmu,Espacing))==Ci*(-dmu)
    return dmu


def Qsys_decompositor(fsc,sys,tol=1e-5):
    #delete the idx from self.Qprime if the local ni==0
    return 
def relax_I():
    #relax set one
    return 

def relax_II():

    return

def relax_III():
    return